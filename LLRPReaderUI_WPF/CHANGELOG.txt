================================================================================
                        LLRP Reader UI 更新日志
================================================================================

版本: 1.0.15-Alpha-260513
发布日期: 2026-05-13
================================================================================

【新增功能】

1. SQLite 原始帧日志自动过期清理
   - 应用启动时自动清理超过 7 天的原始帧日志记录
   - 防止 SQLite 数据库无限增长，减少磁盘占用

2. 标签报告来源区分
   - Tag 新增 AccessSpecId、HasAccessOperationResults、ReportSource 属性
   - 新增 TagReportSource 枚举（Inventory / AttachedData / TagOperation / Unknown）
   - 支持区分盘点报告、附加数据报告和标签操作报告的来源

【优化改进】

1. 天线状态实时显示
   - 状态栏天线状态从固定的"--"改为动态显示各端口连接状态
   - 格式为"1:连 2:断"，按端口号排序，直观反映天线工作情况

2. 盘点标签列表行为优化
   - 新标签追加到列表末尾而非插入顶部，避免列表频繁跳动
   - InventoryTagItemViewModel 属性改为 ObservableProperty 响应式绑定
   - 标签数据更新时 UI 实时刷新，无需手动替换行

3. 主题与语言初始化优化
   - 默认主题（浅色）和默认语言（中文）时跳过初始化加载，提升启动速度

【缺陷修复】

1. 过滤参数 BitCount 显示修复
   - 修复从设备读取过滤参数时 BitCount 未正确赋值的问题
   - TagFilter1、TagFilter2 及 TagSelectFilters 列表均已修复

================================================================================

版本: 1.0.14-Alpha-260508
发布日期: 2026-05-08
================================================================================

【优化改进】

1. WPF 主题与样式全面升级
   - 统一控件高度、按钮宽度和节标题样式，视觉一致性显著提升
   - TextBox 左对齐留白与 ComboBox 保持一致
   - 统一 AutoCompleteBox 与 TextBox 样式
   - ReadWriteView、LogView 标签页圆角统一优化
   - 盘点统计仪表盘样式优化，数据展示更清晰
   - 状态栏状态芯片样式优化，状态指示更直观
   - 按钮样式现代化，交互反馈更明确

2. 配置页面布局优化
   - UI 配置分组重新整理，GPIO 配置整合进设置页
   - 配置布局响应性优化，窗口缩放时布局自适应更好
   - 状态感知过滤模式独立拆分，配置逻辑更清晰
   - 状态感知设置项在设备不支持时保持可见（灰化提示），方便用户了解设备能力

3. LLRP 原始消息日志性能优化
   - 改为批量写入数据库，避免高频率消息导致 UI 卡顿
   - 修复日志丢失问题，确保原始帧日志完整性

4. 主题打磨
   - 浅色/深色主题细节打磨，提升整体视觉品质
   - 浅色主题下状态感知过滤操作项可见性完善

【缺陷修复】

1. 还原参数时寻卡参数 UI 不更新
   - 修复从设备还原参数后，寻卡参数（Q 值、Session 等）界面不刷新的问题

2. 原始帧日志容错
   - 设备禁用原始帧上报时，不再产生异常日志

================================================================================

版本: 1.0.13-Alpha-260424
发布日期: 2026-04-24
================================================================================

【优化改进】

1. UI 显示调整
   - TagPopulationEstimate 的 Q 值关系说明移动到输入框右侧，显示“约对应标签数 = 2^Q - 1”
   - 上报模式下拉列表补充显示对应的 LLRP 参数值（Trigger / N）
   - TagFilterMode 按模式动态控制过滤器显示：OnlyFilter1/OnlyFilter2/AND/OR 仅显示标签筛选1/2，UseTagSelectFilters 显示可增删的过滤条件列表
   - 主题样式统一优化（浅色/深色），修复导航区、标题区、状态区在不同主题下的显示一致性
   - 日志页与 LLRP 消息页布局和按钮样式优化，筛选区与 Tab Header 视觉统一

2. 状态感知过滤增强
   - 在 TagSelectFilters 中新增 `UseStateAwareFilterAction` 开关
   - 新增 `StateAwareTarget`、`StateAwareAction` 配置项（仅在开启 InventoryStateAware 时显示）
   - 设置读取/保存与下发链路已打通：支持生成并解析 `C1G2TagInventoryStateAwareFilterAction`
   - 修复相关显示刷新问题，确保设置变更后配置区可见性及时更新
   - 使用说明（面向用户）：
     1) 在【参数配置】页面开启 InventoryStateAware，并保存参数到设备
     2) 在【盘点配置】页面将 Filter Mode 切换为 UseTagSelectFilters
     3) 点击“新增过滤条件”，勾选 UseStateAwareFilterAction
     4) 选择 StateAwareTarget 与 StateAwareAction，保存后开始盘点
     5) 如未显示该配置项，请先确认设备支持 CanDoTagInventoryStateAwareSingulation
   - 推荐起步配置：
     - StateAwareTarget = SL
     - StateAwareAction = AssertSLOrA_DeassertSLOrB
   - 参数含义说明：
     - StateAwareTarget：指定要操作的状态位，`SL` 为标签 SL 标志；`S0/S1/S2/S3` 为对应 Session 的 A/B 状态位
     - StateAwareAction：定义“匹配/不匹配”两种情况下的状态动作，格式为 `X_Y`（X=匹配时动作，Y=不匹配时动作）
     - `AssertSLOrA`：置位（SL=1 或 Session 状态置 A）
     - `DeassertSLOrB`：清位（SL=0 或 Session 状态置 B）
     - `Noop`：不改变当前状态
     - `NegateSLOrABBA`：翻转状态（SL 取反或 A/B 互换）
   - 常见动作示例：
     - `AssertSLOrA_DeassertSLOrB`：匹配置位，不匹配清位（推荐起步）
     - `AssertSLOrA_Noop`：匹配置位，不匹配保持不变
     - `Noop_DeassertSLOrB`：匹配不变，不匹配清位

================================================================================

版本: 1.0.12-Alpha-260422
发布日期: 2026-04-22
================================================================================

【优化改进】

1. UI 显示调整
   - 删除 IndividualUnbuffered 选项
   - 获取 GPIO 状态时强制从设备同步一次数据
   - 暂时移除上报数据的筛选，所有时间段的数据均在 UI 上显示

================================================================================

版本: 1.0.11-Alpha-260421
发布日期: 2026-04-21
================================================================================

【新增功能】

1. 高级标签操作超时机制
   - 为块擦除、锁定、销毁等高级操作增加了 5 秒超时控制
   - 操作超时自动提示“未匹配到目标标签”并解除忙碌状态
   - 增强了操作过程中的日志记录与附加数据 (AO) 状态恢复
   - 完善了中英文本地化支持

2. 状态感知盘存支持
   - 支持解析 CanDoTagInventoryStateAwareSingulation 能力
   - 增加 InventoryStateAware 开关和 InventoryTarget (A/B) 配置项
   - 增加 InventorySearchMode (SL / Not_SL) 搜索模式配置项
   - GetAiSpec 根据设备能力自动决定是否开启状态感知
   - UI 界面增加了相关配置项和本地化文本

3. 参数配置扩展
   - 支持天线 PARAM_RFTransmitter 配置

【优化改进】

1. 参数配置优化
   - RF Mode 下拉列表增加 "Pn" 前缀显示（如：P0 - 113 - [详情]），提升识别直观性
   - 跳频表和发射频率的显示精度提升到小数点后三位 (F3)
   - HopTableId 和 ChannelIndex 的默认值修改为 1

================================================================================

版本: 1.0.10-Alpha-130426
发布日期: 2026-04-13
================================================================================

【缺陷修复】

1. 心跳超时误判修复
   - 修复了设备刚连接时，如果设备本身已开启心跳，可能触发误判并断开连接的问题
   - 为心跳间隔增加了下限容错机制，防止由于设备处理延迟导致的意外断线

================================================================================

版本: 1.0.9-Alpha-270327
发布日期: 2025-03-27
================================================================================

【新增功能】

1. LLRP消息树显示功能
   - LLRP消息历史页面支持树形结构展示
   - 更清晰地查看消息层级和参数结构

2. 默认RfMode自动选择
   - 获取设备配置时自动选择第一个可用的RfMode
   - 简化用户配置流程

【优化改进】

1. 默认Session值调整
   - 默认值从Session 2改为Session 0
   - 适用于大多数盘点场景

2. 日志输出优化
   - 去除日志消息末尾多余的换行符
   - 日志显示更加整洁

3. 清空历史记录功能
   - 支持清空SQLite历史消息记录

================================================================================

版本: 1.0.8-Alpha-260325
发布日期: 2025-03-26
================================================================================

【新增功能】

1. 主题切换功能
   - 支持浅色/深色主题动态切换
   - 无需重启应用程序即可生效
   - 工具栏提供快捷切换按钮

2. 语言切换功能（中英文）
   - 支持中文(zh-CN)和英文(en-US)界面切换
   - 无需重启应用程序即可生效
   - 所有页面文本均已本地化支持
   - 工具栏提供快捷切换按钮

3. 全页面中英文本地化
   - 设备连接页面
   - 参数配置页面
   - GPIO配置页面
   - 盘点配置页面
   - 盘点操作页面
   - 读写操作页面
   - 高级标签操作页面
   - 日志页面
   - LLRP消息历史页面
   - 导航菜单
   - 状态栏

【优化改进】

1. 状态栏语言切换实时更新
   - 设备状态、盘点状态、天线、GPI、GPO、MAC等标签支持语言切换

2. 导航菜单语言切换实时更新


