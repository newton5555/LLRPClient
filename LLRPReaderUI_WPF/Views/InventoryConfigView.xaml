<UserControl x:Class="LLRPReaderUI_WPF.Views.InventoryConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:LLRPReaderUI_WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:InventoryConfigViewModel}"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="自动开始配置" Margin="0,0,0,12">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="220" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="模式" />
                        <ComboBox Grid.Row="0" Grid.Column="1" Height="30" ItemsSource="{Binding AutoStartModes}" SelectedItem="{Binding AutoStartMode}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="GPI端口号" />
                        <TextBox Grid.Row="2" Grid.Column="1" Height="30" Text="{Binding AutoStartGpiPortNumber}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="GPI电平" />
                        <CheckBox Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding AutoStartGpiLevel}" />

                        <TextBlock Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Text="首次延时(ms)" />
                        <TextBox Grid.Row="6" Grid.Column="1" Height="30" Text="{Binding AutoStartFirstDelayInMs}" />

                        <TextBlock Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Text="周期(ms)" />
                        <TextBox Grid.Row="8" Grid.Column="1" Height="30" Text="{Binding AutoStartPeriodInMs}" />

                        <TextBlock Grid.Row="10" Grid.Column="0" VerticalAlignment="Center" Text="UTC时间戳" />
                        <TextBox Grid.Row="10" Grid.Column="1" Height="30" Text="{Binding AutoStartUtcTimestamp}" />
                    </Grid>
                </GroupBox>

                <GroupBox Header="自动停止配置" Margin="0,0,0,12">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="220" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="模式" />
                        <ComboBox Grid.Row="0" Grid.Column="1" Height="30" ItemsSource="{Binding AutoStopModes}" SelectedItem="{Binding AutoStopMode}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="持续时长(ms)" />
                        <TextBox Grid.Row="2" Grid.Column="1" Height="30" Text="{Binding AutoStopDurationInMs}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="GPI端口号" />
                        <TextBox Grid.Row="4" Grid.Column="1" Height="30" Text="{Binding AutoStopGpiPortNumber}" />

                        <TextBlock Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Text="GPI电平" />
                        <CheckBox Grid.Row="6" Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding AutoStopGpiLevel}" />

                        <TextBlock Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Text="超时" />
                        <TextBox Grid.Row="8" Grid.Column="1" Height="30" Text="{Binding AutoStopTimeout}" />
                    </Grid>
                </GroupBox>

                <GroupBox Header="筛选配置" Margin="0,0,0,12">
                    <StackPanel Margin="10">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="220" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="模式" />
                            <ComboBox Grid.Column="1" Height="30" ItemsSource="{Binding TagFilterModes}" SelectedItem="{Binding FilterMode}" />
                        </Grid>

                        <GroupBox Header="标签筛选1 (TagFilter1)" Margin="0,0,0,8">
                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="200" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="MemoryBank" />
                                <ComboBox Grid.Row="0" Grid.Column="1" Height="28" ItemsSource="{Binding MemoryBanks}" SelectedItem="{Binding Filter1MemoryBank}" />
                                <TextBlock Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Text="FilterOp" />
                                <ComboBox Grid.Row="0" Grid.Column="3" Height="28" ItemsSource="{Binding TagFilterOps}" SelectedItem="{Binding Filter1FilterOp}" />

                                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="BitPointer" />
                                <TextBox Grid.Row="2" Grid.Column="1" Height="28" Text="{Binding Filter1BitPointer}" />
                                <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Text="BitCount" />
                                <TextBox Grid.Row="2" Grid.Column="3" Height="28" Text="{Binding Filter1BitCount}" />

                                <TextBlock  Grid.Row="4" Margin="0,0,0,4" Text="TagFilter1 TagMask (HEX)" Grid.ColumnSpan="4"/>
                                <TextBox Grid.Row="5" Margin="0,0,0,8" Height="30" Text="{Binding Filter1TagMask}" Grid.ColumnSpan="4"/>
                            </Grid>
                        </GroupBox>

                      

                        <GroupBox Header="标签筛选2 (TagFilter2)" Margin="0,0,0,8">
                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="200" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="MemoryBank" />
                                <ComboBox Grid.Row="0" Grid.Column="1" Height="28" ItemsSource="{Binding MemoryBanks}" SelectedItem="{Binding Filter2MemoryBank}" />
                                <TextBlock Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Text="FilterOp" />
                                <ComboBox Grid.Row="0" Grid.Column="3" Height="28" ItemsSource="{Binding TagFilterOps}" SelectedItem="{Binding Filter2FilterOp}" />

                                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="BitPointer" />
                                <TextBox Grid.Row="2" Grid.Column="1" Height="28" Text="{Binding Filter2BitPointer}" />
                                <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Text="BitCount" />
                                <TextBox Grid.Row="2" Grid.Column="3" Height="28" Text="{Binding Filter2BitCount}" />

                                <TextBlock  Grid.Row="4" Margin="0,0,0,4" Text="TagFilter2 TagMask (HEX)" Grid.ColumnSpan="4"/>
                                <TextBox Grid.Row="5" Margin="0,0,0,8" Height="30" Text="{Binding Filter2TagMask}" Grid.ColumnSpan="4"/>
                            </Grid>
                        </GroupBox>

                      

                        <GroupBox Header="标签选择筛选 (TagSelectFilters)" Margin="0,6,0,0">
                            <StackPanel Margin="8">
                                <Button Width="120" Height="30" Content="新增筛选" Command="{Binding AddTagSelectFilterCommand}" />
                                <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding TagSelectFilters}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,0,8" Padding="8" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="130" />
                                                        <ColumnDefinition Width="170" />
                                                        <ColumnDefinition Width="130" />
                                                        <ColumnDefinition Width="170" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="8" />
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="8" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="MemoryBank" />
                                                    <ComboBox Grid.Row="0" Grid.Column="1" Height="26"
                                                              ItemsSource="{Binding DataContext.MemoryBanks, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedItem="{Binding MemoryBank}" />
                                                    <TextBlock Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Text="位指针(BitPointer)" />
                                                    <TextBox Grid.Row="0" Grid.Column="3" Height="26" Text="{Binding BitPointer}" />
                                                    <Button Grid.Row="0" Grid.Column="4" Width="72" Height="26" Margin="8,0,0,0"
                                                            Content="删除"
                                                            Command="{Binding DataContext.RemoveTagSelectFilterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}" />

                                                    <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="位长度(BitCount)" />
                                                    <TextBox Grid.Row="2" Grid.Column="1" Height="26" Text="{Binding BitCount}" />
                                                    <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Text="MatchAction" />
                                                    <ComboBox Grid.Row="2" Grid.Column="3" Height="26"
                                                              ItemsSource="{Binding DataContext.StateUnawareActions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedItem="{Binding MatchAction}" />

                                                    <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="TagMask" />
                                                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TagMask}" />
                                                    <TextBlock Grid.Row="4" Grid.Column="2" VerticalAlignment="Center" Text="NonMatchAction" />
                                                    <ComboBox Grid.Row="4" Grid.Column="3" Height="26"
                                                              ItemsSource="{Binding DataContext.StateUnawareActions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedItem="{Binding NonMatchAction}" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="上报配置" Margin="0,0,0,12">
                    <StackPanel Margin="10">
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="220" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="上报模式" />
                            <ComboBox Grid.Column="1" Height="30" ItemsSource="{Binding ReportModes}" SelectedItem="{Binding ReportMode}" />
                        </Grid>

                        <UniformGrid Columns="2" Rows="4">
                            <CheckBox Content="包含天线号" IsChecked="{Binding IncludeAntennaPortNumber}" />
                            <CheckBox Content="包含信道" IsChecked="{Binding IncludeChannel}" />
                            <CheckBox Content="包含首次时间" IsChecked="{Binding IncludeFirstSeenTime}" />
                            <CheckBox Content="包含末次时间" IsChecked="{Binding IncludeLastSeenTime}" />
                            <CheckBox Content="包含出现次数" IsChecked="{Binding IncludeSeenCount}" />
                            <CheckBox Content="包含峰值RSSI" IsChecked="{Binding IncludePeakRssi}" />
                            <CheckBox Content="包含PC位" IsChecked="{Binding IncludePcBits}" />
                            <CheckBox Content="包含CRC" IsChecked="{Binding IncludeCrc}" />
                        </UniformGrid>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="附加数据配置" Margin="0,0,0,12">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="220" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="启用附加数据" />
                        <CheckBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding AttachedDataEnabled}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="存储区" />
                        <ComboBox Grid.Row="2" Grid.Column="1" Height="30" ItemsSource="{Binding MemoryBanks}" SelectedItem="{Binding AttachedDataMemoryBank}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="起始字地址" />
                        <TextBox Grid.Row="4" Grid.Column="1" Height="30" Text="{Binding AttachedDataWordPointer}" />

                        <TextBlock Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Text="读取字数" />
                        <TextBox Grid.Row="6" Grid.Column="1" Height="30" Text="{Binding AttachedDataWordCount}" />

                        <TextBlock Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Text="访问密码(HEX)" />
                        <TextBox Grid.Row="8" Grid.Column="1" Height="30" Text="{Binding AttachedDataAccessPassword}" />
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Height="32" Width="120" Margin="0,0,8,0" Content="读取寻卡配置" Command="{Binding QueryInventoryConfigCommand}" />
            <Button Height="32" Width="120" Content="保存寻卡配置" Command="{Binding SaveInventoryConfigCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="3" Margin="4,0,0,0" VerticalAlignment="Center" Text="{Binding OperationResult}" />
    </Grid>
</UserControl>
