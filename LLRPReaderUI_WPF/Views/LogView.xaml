<UserControl
    x:Class="LLRPReaderUI_WPF.Views.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
    xmlns:converters="clr-namespace:LLRPReaderUI_WPF.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:LLRPReaderUI_WPF.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:LogViewModel}"
    d:Height="600"
    d:Width="800"
    mc:Ignorable="d">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BooleanToStarGridLengthConverter x:Key="BoolToStarGridLengthConverter" />
        
        <Style x:Key="LogTabHeaderTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="HeaderBorder" Background="{TemplateBinding Background}" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0A000000" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{DynamicResource AppCardStyle}" Margin="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header: Stats and Global Actions -->
            <Border Grid.Row="0" Padding="16" Background="{DynamicResource MahApps.Brushes.ThemeBackground}" BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Database" IconFont="Auto" Foreground="{StaticResource IconVioletBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="日志统计" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <TextBlock VerticalAlignment="Center" FontSize="13">
                            <Run Text="日志总数: " Foreground="{DynamicResource TextMutedBrush}" />
                            <Run FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Text="{Binding TotalLogCount, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="32,0,0,0" Orientation="Horizontal" VerticalAlignment="Bottom">
                        <CheckBox Margin="0,0,20,0" IsChecked="{Binding ShowOperationLogs}">
                            <StackPanel Orientation="Horizontal">
                                <fa:IconBlock Icon="ClipboardList" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center" Foreground="{StaticResource IconTealBrush}"/>
                                <TextBlock Text="操作日志" VerticalAlignment="Center" FontSize="12"/>
                                <TextBlock Margin="4,0,0,0" Text="{Binding OperationEntries.Count, Mode=OneWay, StringFormat=({0})}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" VerticalAlignment="Center"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox Margin="0,0,20,0" IsChecked="{Binding ShowLlrpMessageLogs}">
                            <StackPanel Orientation="Horizontal">
                                <fa:IconBlock Icon="SatelliteDish" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center" Foreground="{StaticResource IconAccentBrush}"/>
                                <TextBlock Text="LLRP 消息" VerticalAlignment="Center" FontSize="12"/>
                                <TextBlock Margin="4,0,0,0" Text="{Binding LlrpMessageEntries.Count, Mode=OneWay, StringFormat=({0})}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" VerticalAlignment="Center"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox IsChecked="{Binding ShowRawFrameLogs}">
                            <StackPanel Orientation="Horizontal">
                                <fa:IconBlock Icon="Code" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center" Foreground="{StaticResource IconSuccessBrush}"/>
                                <TextBlock Text="原始报文" VerticalAlignment="Center" FontSize="12"/>
                                <TextBlock Margin="4,0,0,0" Text="{Binding RawPacketEntries.Count, Mode=OneWay, StringFormat=({0})}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" VerticalAlignment="Center"/>
                            </StackPanel>
                        </CheckBox>
                    </StackPanel>

                    <Button Grid.Column="2" VerticalAlignment="Center" Height="36" Padding="16,0" Command="{Binding ClearLogsCommand}" Style="{StaticResource FlatButtonOutline}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconBlock Icon="TrashCan" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="清空全部" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Log Content Area -->
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
                <!-- Operation Logs -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="14" Icon="ClipboardList" IconFont="Auto" VerticalAlignment="Center"/>
                            <TextBlock Style="{StaticResource LogTabHeaderTitleStyle}" Text="操作日志" />
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Visibility="{Binding ShowOperationLogs, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" Margin="12,8" HorizontalAlignment="Right" Height="28" Padding="12,0" Command="{Binding ClearOperationLogsCommand}" Content="清空" Style="{StaticResource FlatButtonOutline}" />
                        <ListBox x:Name="OperationListBox" Grid.Row="1" Background="Transparent" BorderThickness="0" FontFamily="Consolas, Courier New" FontSize="11" ItemsSource="{Binding OperationEntries, Mode=OneWay}" VirtualizingPanel.ScrollUnit="Pixel" SelectionMode="Extended" />
                    </Grid>
                </TabItem>

                <!-- LLRP Message Logs -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="14" Icon="SatelliteDish" IconFont="Auto" VerticalAlignment="Center"/>
                            <TextBlock Style="{StaticResource LogTabHeaderTitleStyle}" Text="LLRP 消息" />
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Visibility="{Binding ShowLlrpMessageLogs, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" Margin="12,8" HorizontalAlignment="Right" Height="28" Padding="12,0" Command="{Binding ClearLlrpMessageLogsCommand}" Content="清空" Style="{StaticResource FlatButtonOutline}" />
                        <ListBox x:Name="LlrpMessageListBox" Grid.Row="1" Background="Transparent" BorderThickness="0" FontFamily="Consolas, Courier New" FontSize="11" ItemsSource="{Binding LlrpMessageEntries, Mode=OneWay}" VirtualizingPanel.ScrollUnit="Pixel" SelectionMode="Extended" />
                    </Grid>
                </TabItem>

                <!-- Raw Packet Logs -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="14" Icon="Code" IconFont="Auto" VerticalAlignment="Center"/>
                            <TextBlock Style="{StaticResource LogTabHeaderTitleStyle}" Text="原始报文" />
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Visibility="{Binding ShowRawFrameLogs, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" Margin="12,8" HorizontalAlignment="Right" Height="28" Padding="12,0" Command="{Binding ClearRawFrameLogsCommand}" Content="清空" Style="{StaticResource FlatButtonOutline}" />
                        <ListBox x:Name="RawPacketListBox" Grid.Row="1" Background="Transparent" BorderThickness="0" FontFamily="Consolas, Courier New" FontSize="11" ItemsSource="{Binding RawPacketEntries, Mode=OneWay}" VirtualizingPanel.ScrollUnit="Pixel" SelectionMode="Extended" />
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</UserControl>
