<UserControl x:Class="LLRPReaderUI_WPF.Views.ReadWriteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:viewmodels="clr-namespace:LLRPReaderUI_WPF.ViewModels" 
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             d:DataContext="{d:DesignInstance Type=viewmodels:ReadWriteViewModel}"
             mc:Ignorable="d">
    <Border Style="{DynamicResource AppCardStyle}" Margin="16">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel MaxWidth="900">
                <!-- Parameters Section -->
                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Sliders" IconFont="Auto" Foreground="{StaticResource IconVioletBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource ReadWrite.ReadConfig}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>

                        <Grid Margin="8,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Row="0" Grid.ColumnSpan="2" Background="{DynamicResource IconInfoBrush}" Opacity="0.1" CornerRadius="4" Padding="8">
                                <TextBlock Foreground="{DynamicResource IconInfoBrush}" Text="{DynamicResource ReadWrite.Legend}" TextWrapping="Wrap" FontSize="12"/>
                            </Border>

                            <TextBlock Grid.Row="2" Text="{DynamicResource ReadWrite.TargetTagType}" VerticalAlignment="Center" />
                            <ComboBox Grid.Row="2" Grid.Column="1" Height="32" ItemsSource="{Binding TargetTagBanks}" SelectedItem="{Binding SelectedTargetTagBank}" ToolTip="{DynamicResource ReadWrite.TargetTagTooltip}" />

                            <TextBlock Grid.Row="4" Text="{DynamicResource ReadWrite.TargetTagData}" VerticalAlignment="Center" FontWeight="SemiBold" />
                            <Border Grid.Row="4" Grid.Column="1" Height="32" BorderBrush="{DynamicResource WriteRequiredForeground}" BorderThickness="1">
                                <TextBox VerticalContentAlignment="Center" Padding="8,0" Background="{DynamicResource BothRequiredBackground}" BorderBrush="{DynamicResource ReadRequiredForeground}" Text="{Binding TargetTagData, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource ReadWrite.TargetTagTooltip}" />
                            </Border>

                            <TextBlock Grid.Row="6" Text="{DynamicResource ReadWrite.MemoryBank}" VerticalAlignment="Center" />
                            <ComboBox Grid.Row="6" Grid.Column="1" Height="32" ItemsSource="{Binding MemoryBanks}" SelectedItem="{Binding SelectedMemoryBank}" />

                            <TextBlock Grid.Row="8" Text="{DynamicResource ReadWrite.WordStartAddress}" VerticalAlignment="Center" FontWeight="SemiBold" />
                            <Border Grid.Row="8" Grid.Column="1" Height="32" BorderBrush="{DynamicResource WriteRequiredForeground}" BorderThickness="1">
                                <TextBox VerticalContentAlignment="Center" Padding="8,0" Background="{DynamicResource BothRequiredBackground}" BorderBrush="{DynamicResource ReadRequiredForeground}" Text="{Binding WordPointer, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource ReadWrite.WordPointerTooltip}" />
                            </Border>

                            <TextBlock Grid.Row="10" Text="{DynamicResource ReadWrite.WordCount}" VerticalAlignment="Center" Foreground="{DynamicResource ReadRequiredForeground}" FontWeight="SemiBold" />
                            <TextBox Grid.Row="10" Grid.Column="1" Height="32" Background="{DynamicResource ReadRequiredBackground}" BorderBrush="{DynamicResource ReadRequiredForeground}" Text="{Binding WordCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource ReadWrite.WordCountTooltip}" />

                            <TextBlock Grid.Row="12" Text="{DynamicResource ReadWrite.WriteData}" VerticalAlignment="Center" Foreground="{DynamicResource WriteRequiredForeground}" FontWeight="SemiBold" />
                            <TextBox Grid.Row="12" Grid.Column="1" Height="32" Background="{DynamicResource WriteRequiredBackground}" BorderBrush="{DynamicResource WriteRequiredForeground}" Text="{Binding WriteData, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource ReadWrite.WriteDataTooltip}" />

                            <TextBlock Grid.Row="14" Text="{DynamicResource ReadWrite.AccessPassword}" VerticalAlignment="Center" />
                            <TextBox Grid.Row="14" Grid.Column="1" Height="32" Text="{Binding AccessPassword, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource ReadWrite.AccessPasswordTooltip}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Actions Section -->
                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Bolt" IconFont="Auto" Foreground="{StaticResource IconTealBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource ReadWrite.Operations}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        
                        <StackPanel Margin="8,0">
                            <UniformGrid Columns="3" Margin="0,0,0,12">
                                <Button Height="36" Margin="0,0,8,0" Command="{Binding ReadMemoryCommand}" Style="{StaticResource FlatButtonOutline}">
                                    <StackPanel Orientation="Horizontal">
                                        <fa:IconBlock Icon="Download" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                                        <TextBlock Text="{DynamicResource ReadWrite.ReadMemory}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                                <Button Height="36" Margin="4,0,4,0" Command="{Binding WriteMemoryCommand}" Style="{StaticResource FlatButtonPrimary}">
                                    <StackPanel Orientation="Horizontal">
                                        <fa:IconBlock Icon="Upload" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                                        <TextBlock Text="{DynamicResource ReadWrite.WriteMemory}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                                <Button Height="36" Margin="8,0,0,0" Command="{Binding ClearDataCommand}" Style="{StaticResource FlatButtonOutline}">
                                    <StackPanel Orientation="Horizontal">
                                        <fa:IconBlock Icon="Eraser" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                                        <TextBlock Text="{DynamicResource ReadWrite.ClearData}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                            </UniformGrid>
                            
                            <CheckBox Margin="0,8" VerticalAlignment="Center" Content="{DynamicResource ReadWrite.BlockWrite}" IsChecked="{Binding IsBlockWriteChecked}" IsEnabled="{Binding IsBlockWriteSupported}" ToolTip="{DynamicResource ReadWrite.BlockWriteTooltip}" />
                            
                            <Border Background="{DynamicResource MahApps.Brushes.Accent}" Opacity="0.05" CornerRadius="4" Padding="8" Margin="0,8,0,0">
                                <TextBlock Text="{Binding OperationResult}" Foreground="{DynamicResource PrimaryBrush}" TextWrapping="Wrap" FontSize="12"/>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Result Section -->
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="FileCode" IconFont="Auto" Foreground="{StaticResource IconInfoBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource ReadWrite.ReadResult}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Margin="8,0">
                            <TextBlock Text="{DynamicResource ReadWrite.HexData}" Margin="0,0,0,8" Foreground="{DynamicResource TextMutedBrush}" />
                            <TextBox Height="120" VerticalContentAlignment="Top" Padding="8" Text="{Binding ReadData, Mode=OneWay}" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" FontFamily="Consolas, Courier New" Background="Transparent" BorderThickness="0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
