<UserControl x:Class="LLRPReaderUI_WPF.Views.AdvancedTagOpsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:LLRPReaderUI_WPF.ViewModels" xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             d:DataContext="{d:DesignInstance Type=viewmodels:AdvancedTagOpsViewModel}"
             mc:Ignorable="d">
    <Border Style="{DynamicResource AppCardStyle}" Margin="16">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel MaxWidth="900">
                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16" Visibility="Collapsed">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Book" IconFont="Auto" Foreground="{StaticResource IconInfoBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource AdvancedTagOps.StandardSummary}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Margin="8,0">
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="15.2.1.3.2 C1G2 OpSpec Parameters" />
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="15.2.1.3.2.1 C1G2Read" />
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="15.2.1.3.2.2 C1G2Write" />
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="15.2.1.3.2.3 C1G2Kill" />
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="15.2.1.3.2.4 C1G2Lock" />
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="15.2.1.3.2.5 C1G2BlockErase" />
                            <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="15.2.1.3.2.6 C1G2BlockWrite" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Gear" IconFont="Auto" Foreground="{StaticResource IconVioletBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource AdvancedTagOps.CommonParams}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <Grid Margin="8,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Row="0" Grid.ColumnSpan="2" Background="{DynamicResource IconDangerBrush}" Opacity="0.1" CornerRadius="4" Padding="8">
                                <TextBlock Foreground="{DynamicResource IconDangerBrush}" Text="{DynamicResource AdvancedTagOps.Warning}" TextWrapping="Wrap" FontSize="12"/>
                            </Border>

                            <TextBlock Grid.Row="2" Text="{DynamicResource AdvancedTagOps.TargetTagType}" VerticalAlignment="Center" />
                            <ComboBox Grid.Row="2" Grid.Column="1" Height="32" ItemsSource="{Binding TargetTagBanks}" SelectedItem="{Binding SelectedTargetTagBank}" />

                            <TextBlock Grid.Row="4" Text="{DynamicResource AdvancedTagOps.TargetTagValue}" VerticalAlignment="Center" />
                            <TextBox Grid.Row="4" Grid.Column="1" Height="32" Text="{Binding TargetTagData, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Grid.Row="6" Text="{DynamicResource AdvancedTagOps.AccessPassword}" VerticalAlignment="Center" />
                            <TextBox Grid.Row="6" Grid.Column="1" Height="32" Text="{Binding AccessPassword, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Grid.Row="8" Text="{DynamicResource AdvancedTagOps.KillPassword}" VerticalAlignment="Center" />
                            <TextBox Grid.Row="8" Grid.Column="1" Height="32" Text="{Binding KillPassword, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                        <TextBlock Margin="8,12,8,0" Foreground="{DynamicResource TextMutedBrush}" Text="{DynamicResource AdvancedTagOps.PermalockNote}" FontSize="11" FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Eraser" IconFont="Auto" Foreground="{StaticResource IconWarningBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource AdvancedTagOps.BlockErase}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <Grid Margin="8,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"
                                      Text="{Binding IsBlockEraseSupported, StringFormat=Device Capability: C1G2BlockErase {0}}" />

                            <TextBlock Grid.Row="2" Text="{DynamicResource AdvancedTagOps.MemoryBank}" VerticalAlignment="Center" />
                            <ComboBox Grid.Row="2" Grid.Column="1" Height="32" ItemsSource="{Binding MemoryBanks}" SelectedItem="{Binding SelectedMemoryBank}" />

                            <TextBlock Grid.Row="4" Text="{DynamicResource AdvancedTagOps.WordStartAddr}" VerticalAlignment="Center" />
                            <TextBox Grid.Row="4" Grid.Column="1" Height="32" Text="{Binding WordPointer, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Grid.Row="6" Text="{DynamicResource AdvancedTagOps.WordCount}" VerticalAlignment="Center" />
                            <TextBox Grid.Row="6" Grid.Column="1" Height="32" Text="{Binding WordCount, UpdateSourceTrigger=PropertyChanged}" />

                            <Button Grid.Row="6" Grid.Column="2" Height="32" Margin="10,0,0,0"
                                    Content="{DynamicResource AdvancedTagOps.ExecuteBlockErase}"
                                    IsEnabled="{Binding IsBlockEraseSupported}"
                                    Command="{Binding BlockEraseCommand}" 
                                    Style="{StaticResource FlatButtonPrimary}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Lock" IconFont="Auto" Foreground="{StaticResource IconAccentBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource AdvancedTagOps.LockOp}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <Grid Margin="8,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Text="{DynamicResource AdvancedTagOps.LockTarget}" VerticalAlignment="Center" />
                            <ComboBox Grid.Row="0" Grid.Column="1" Height="32" ItemsSource="{Binding LockBanks}" SelectedItem="{Binding SelectedLockBank}" />

                            <TextBlock Grid.Row="2" Text="{DynamicResource AdvancedTagOps.LockAction}" VerticalAlignment="Center" />
                            <ComboBox Grid.Row="2" Grid.Column="1" Height="32" ItemsSource="{Binding LockActions}" SelectedItem="{Binding SelectedLockAction}" />

                            <Button Grid.Row="2" Grid.Column="2" Height="32" Margin="10,0,0,0" 
                                    Content="{DynamicResource AdvancedTagOps.ExecuteLock}" 
                                    Command="{Binding LockCommand}"
                                    Style="{StaticResource FlatButtonPrimary}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="Skull" IconFont="Auto" Foreground="{DynamicResource IconDangerBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource AdvancedTagOps.KillOp}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Margin="8,0" Orientation="Horizontal">
                            <Button Height="32" Width="120" Content="{DynamicResource AdvancedTagOps.ExecuteKill}" Command="{Binding KillCommand}" Style="{DynamicResource FlatButtonDanger}"/>
                            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource IconDangerBrush}" Text="{DynamicResource AdvancedTagOps.KillWarning}" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <fa:IconBlock Margin="0,0,8,0" FontSize="16" Icon="CircleInfo" IconFont="Auto" Foreground="{DynamicResource IconInfoBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource AdvancedTagOps.Result}" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <TextBlock Margin="8,0" Text="{Binding OperationResult}" TextWrapping="Wrap" Foreground="{DynamicResource TextMutedBrush}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
