<UserControl x:Class="LLRPReaderUI_WPF.Views.AdvancedTagOpsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:LLRPReaderUI_WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:AdvancedTagOpsViewModel}"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16" MaxWidth="900">
            <GroupBox Header="标准摘要 (LLRP 1.0.1)" Margin="0,0,0,10">
                <StackPanel Margin="10">
                    <TextBlock Foreground="#4A5568" Text="15.2.1.3.2 C1G2 OpSpec Parameters" />
                    <TextBlock Foreground="#4A5568" Text="15.2.1.3.2.1 C1G2Read" />
                    <TextBlock Foreground="#4A5568" Text="15.2.1.3.2.2 C1G2Write" />
                    <TextBlock Foreground="#4A5568" Text="15.2.1.3.2.3 C1G2Kill" />
                    <TextBlock Foreground="#4A5568" Text="15.2.1.3.2.4 C1G2Lock (含 C1G2LockPayload)" />
                    <TextBlock Foreground="#4A5568" Text="15.2.1.3.2.5 C1G2BlockErase" />
                    <TextBlock Foreground="#4A5568" Text="15.2.1.3.2.6 C1G2BlockWrite" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="公共参数">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Foreground="#7A1F1F" Text="警告：本页包含高风险操作（C1G2Lock/C1G2Kill/C1G2BlockErase）。请在测试标签验证后再上生产环境。" />

                    <TextBlock Grid.Row="2" Text="目标标签类型" VerticalAlignment="Center" />
                    <ComboBox Grid.Row="2" Grid.Column="1" Height="32" ItemsSource="{Binding TargetTagBanks}" SelectedItem="{Binding SelectedTargetTagBank}" />

                    <TextBlock Grid.Row="4" Text="目标标签值" VerticalAlignment="Center" />
                    <TextBox Grid.Row="4" Grid.Column="1" Height="32" Text="{Binding TargetTagData, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="6" Text="访问密码" VerticalAlignment="Center" />
                    <TextBox Grid.Row="6" Grid.Column="1" Height="32" Text="{Binding AccessPassword, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="8" Text="Kill 密码" VerticalAlignment="Center" />
                    <TextBox Grid.Row="8" Grid.Column="1" Height="32" Text="{Binding KillPassword, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="10" Grid.ColumnSpan="2" Foreground="#4A5568" Text="标准 C1G2 中 Permalock/Permaunlock 通过 C1G2LockPayload.Privilege 表达；本页的 Lock 动作已覆盖。" />
                </Grid>
            </GroupBox>

            <GroupBox Header="BlockErase" Margin="0,10,0,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0"
                               Grid.ColumnSpan="3"
                               Foreground="#4A5568"
                              Text="{Binding IsBlockEraseSupported, StringFormat=设备能力: C1G2BlockErase {0}}" />

                    <TextBlock Grid.Row="2" Text="内存区" VerticalAlignment="Center" />
                    <ComboBox Grid.Row="2" Grid.Column="1" Height="32" ItemsSource="{Binding MemoryBanks}" SelectedItem="{Binding SelectedMemoryBank}" />

                    <TextBlock Grid.Row="4" Text="Word 起始地址" VerticalAlignment="Center" />
                    <TextBox Grid.Row="4" Grid.Column="1" Height="32" Text="{Binding WordPointer, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="6" Text="Word 数量" VerticalAlignment="Center" />
                    <TextBox Grid.Row="6" Grid.Column="1" Height="32" Text="{Binding WordCount, UpdateSourceTrigger=PropertyChanged}" />

                    <Button Grid.Row="6"
                            Grid.Column="2"
                            Height="36"
                            Margin="10,0,0,0"
                            Content="执行 BlockErase"
                            IsEnabled="{Binding IsBlockEraseSupported}"
                            Command="{Binding BlockEraseCommand}" />
                </Grid>
            </GroupBox>

            <GroupBox Header="Lock" Margin="0,10,0,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="锁定目标" VerticalAlignment="Center" />
                    <ComboBox Grid.Row="0" Grid.Column="1" Height="32" ItemsSource="{Binding LockBanks}" SelectedItem="{Binding SelectedLockBank}" />

                    <TextBlock Grid.Row="2" Text="锁定动作" VerticalAlignment="Center" />
                    <ComboBox Grid.Row="2" Grid.Column="1" Height="32" ItemsSource="{Binding LockActions}" SelectedItem="{Binding SelectedLockAction}" />

                    <Button Grid.Row="2" Grid.Column="2" Height="36" Margin="10,0,0,0" Content="执行 Lock" Command="{Binding LockCommand}" />
                </Grid>
            </GroupBox>

            <GroupBox Header="Kill" Margin="0,10,0,0">
                <StackPanel Margin="10" Orientation="Horizontal">
                    <Button Height="36" Width="150" Content="执行 Kill" Command="{Binding KillCommand}" />
                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#7A1F1F" Text="危险：Kill 成功后标签永久失效" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="结果" Margin="0,10,0,0">
                <TextBlock Margin="10" Text="{Binding OperationResult}" TextWrapping="Wrap" />
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
